document.querySelectorAll('.thumb').forEach(btn=>btn.addEventListener('click',()=>{const g=btn.closest('.gallery');const src=btn.querySelector('img')?.src;if(src&&g){g.querySelector('.galleryMain img').src=src;g.querySelectorAll('.thumb').forEach(x=>x.classList.remove('active'));btn.classList.add('active')}}));
const toast=document.getElementById('toast');function showToast(title,text){if(!toast)return;toast.querySelector('b').textContent=title;toast.querySelector('p').textContent=text;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3600)}
document.querySelectorAll('.consultBtn').forEach(b=>b.addEventListener('click',()=>showToast('Consulta preparada','Completa los datos del vehículo y la referencia. La conexión con WhatsApp/soporte se añadirá en el bloque funcional.')));
document.querySelectorAll('.buyBtn').forEach(b=>b.addEventListener('click',()=>showToast('Servicio configurado','La ficha visual y técnica ya está lista. El checkout y la generación real del pedido se conectarán en el siguiente bloque funcional.')));
document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active')}));